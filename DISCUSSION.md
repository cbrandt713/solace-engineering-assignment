There is still a TON left to be done in this project, but I was hitting the time limit faster than I thought.

Things I didn't get to finish just yet:

- Create a dropdown list for the specialty search instead of a text search.
- Location search should probably be some sort of map interface / text search combo in a highly advanced app.
- Improve the UX of the table, the default way was definitely not the best method, as well as better styling.
- Add better design patterns for data searching and filtering.
- Add better abstraction of components.

And more...
